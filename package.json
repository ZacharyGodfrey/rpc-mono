{
	"name": "rpc-mono",
	"version": "1.0.0",
	"description": "A mono repo containing an RPC-style HTTP API and a statically generated client application",
	"author": "Zachary Godfrey",
	"scripts": {
		"test": "npm run test --workspaces --if-present",
		"build": "npm run build --workspaces --if-present",
		"local": "npm run local --workspaces --if-present"
	},
	"workspaces": [
		"api",
		"client"
	]
}
